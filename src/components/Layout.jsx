import { AppBar } from "./AppBar/AppBar";

export const Layout = () => {
    return (
        <>
            <AppBar />
        
            {/* Will add Suspence and Outlet later when routing is added */}
        </>
    )
}